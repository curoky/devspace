[project]
name = "codespace-workspace-agent"
version = "0.1.0"
description = "Container-side Workspace Agent for codespace"
requires-python = ">=3.14,<3.15"
dependencies = [
    "fastapi==0.139.0",
    "pydantic==2.13.4",
    "uvicorn==0.49.0",
]

[tool.uv]
package = false
