tap 'homebrew/cask'
tap 'homebrew/cask-fonts'
tap 'go-task/tap'
tap 'curoky/tap'

# brew list --cask | xargs -n 1 | sort -u | awk '{ print "\x27"$1"\x27""," }'
casks = [
  'font-fira-code-nerd-font',
  'font-ubuntu-mono-nerd-font',
  'font-lxgw-wenkai',
  'iina',
  'istat-menus',
  'notion',
  'snipaste',
  'squirrel',
  'drawio',
  'raycast',
  # 'firefox',
  # 'google-drive',
  # 'adrive',
  # 'expressvpn',

  # 'android-platform-tools',
  # 'anybar',
  # 'charles',
  # 'chromedriver',
  # 'db-browser-for-sqlite',
  # 'dbeaver-community',
  # 'disk-drill',
  # 'flameshot',
  # 'hyper',
  # 'itsycal',
  # 'michaelvillar-timer',
  # 'motrix',
  # 'netnewswire',
  # 'sketch',
  # 'sol',
  # 'sqlitestudio',
  # 'webtorrent',

  # update by it self
  # 'visual-studio-code',
  # 'warp',
  # 'google-chrome',
]

casks.each do |b|
  cask b
end

brews = [
  'pre-commit', 'prettier', 'yapf', 'shfmt', 'eslint', 'clang-format',
  'python@3', # 'node', 'ruby', 'gcc', 'llvm', 'go', 'rustup-init',
  'openjdk@17', # 'openjdk@8', 'openjdk@11', 'openjdk', 'thrift',
  # 'maven',
  # git
  'git', 'git-absorb', 'git-extras', 'git-lfs', 'git-mv-with-history', 'git-filter-repo',
  # compression
  'bzip2', 'xz', 'zstd', 'zip', 'unzip',
  # network tools
  'netcat', 'lsof', 'connect', 'inetutils', # 'gost',
  # shell tools
  'zsh', 'zsh-bundle', 'starship', # 'asciinema', 'tmux', 'tmuxinator', 'direnv',
  # text editor and tools
  'gettext', 'm4', 'gpatch', 'vim', 'vim-bundle', # 'jq',
  'exa', 'exa-wrapper', 'bat', 'the_silver_searcher', 'fzf',
  # others
  'wget', 'aria2', 'rsync', 'ncdu', 'dotdrop', 'smartmontools',
  'gdu', 'cloc', 'go-task', 'krb5', 'md5deep', 'parallel', 'fd',
  'coreutils', 'rime-bundle',
  # 'gdb', 'opencc', 'gdal', 'gperf',
  # sqlite
  # 'sqlcipher', 'sqlite3', 'sqlite-utils',
]

brews.each do |b|
  brew b
end
